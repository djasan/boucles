const tableau = ["entree1", "entree2", "entree3"];
// Boucles
// foreach

tableau.forEach((element, index) => {
  console.log(element, index);
});

// for

for (let i = 0; i < tableau.length; i++) {
  // const element = tableau[i];
  console.log(i);
}
// while

let n = 0;
while (n < 10) {
  console.log(n);
  n++;

}
